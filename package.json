{
    "name": "Portfolio - Jadoulle Tony",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild src/main.ts --bundle --outfile=public/script.js --format=esm --sourcemap",
        "type-check": "tsc --noEmit"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "esbuild": "^0.28.2",
        "typescript": "^6.0.3"
    }
}